// 'use server'
// import Header from "./Header"
// import { auth } from "@/auth"

// export default async function HeaderServerComp({bgImage, heading, subheading} : {bgImage: string, heading: string, subheading: string}){
//     const session = await auth()
//   return (
//     <Header
//     session = {session}
//     bgImage={bgImage}
//     heading={heading}
//     subheading={subheading}
//     />
//   )
// }